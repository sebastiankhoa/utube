import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
	fonts: {
		heading: "Pacifico, cursive",
		body: "Pacifico, cursive",
	},
	// styles: {
	// 	global: {
	// 		body: {
	// 			bgGradient: "linear(to-r,blue.500,red.500)",
	// 		},
	// 	},
	// },
});

export default theme;
